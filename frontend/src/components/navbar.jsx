// Navbar is not used in pages that have the Sidebar layout.
// This is a legacy component kept for compatibility. The Sidebar.jsx handles navigation.
const Navbar = () => null;
export default Navbar;
